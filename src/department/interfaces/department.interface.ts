// No isActive here - departments are always usable in this simple version.
export interface Department {
  departmentId: string;
  departmentName: string;
  createdAt: string;
  updatedAt: string;
}
